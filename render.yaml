services:
  - type: web
    name: sanam-football-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ISPORTS_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
